{
  "format": "1",
  "runes": {
    "status": {
      "name": "Git Status",
      "description": "Orientation snapshot: discovers all git repositories in the workspace and reports branch, tracking, staged/unstaged files, and recent commits for each.",
      "permissions": {
        "run": {
          "allow": [
            "shell.run:git -C \"*\" rev-parse --abbrev-ref HEAD",
            "shell.run:git -C \"*\" rev-list --count @{u}..HEAD",
            "shell.run:git -C \"*\" rev-list --count HEAD..@{u}",
            "shell.run:git -C \"*\" status --porcelain",
            "shell.run:git -C \"*\" log --oneline -10",
            "shell.run:git -C \"*\" stash list",
            "fs.glob:**/.git"
          ]
        }
      }
    }
  }
}
